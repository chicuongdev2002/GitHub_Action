package n.edu.iuh.fit.controller;import io.jsonwebtoken.Jwts;import io.jsonwebtoken.SignatureAlgorithm;import n.edu.iuh.fit.entity.User;import n.edu.iuh.fit.repositories.RegisterRepository;import n.edu.iuh.fit.serivice.JwtService;import org.springframework.beans.factory.annotation.Autowired;import org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder;import org.springframework.web.bind.annotation.*;import java.util.Date;@RestController@RequestMapping("/register")public class RegisterController {    @Autowired    private RegisterRepository registerRepository;    @Autowired    private JwtService jwtService;    // Đăng kí    @PostMapping()    public User postUser(@RequestBody User user) {        user.setPassword(new BCryptPasswordEncoder().encode(user.getPassword()));        return registerRepository.save(user);    }    @GetMapping("/{id}")    public User getUser(@PathVariable("id") long id) {        return registerRepository.findById(id).get();    }    @PostMapping("/login")    public User login(@RequestBody User user) {        String userName = user.getUserName();        String password = user.getPassword();        User userByUserName = registerRepository.findByUserName(userName);        if (userByUserName != null && new BCryptPasswordEncoder().matches(password, userByUserName.getPassword())) {//            // Tạo token//            String token = jwtService.generateToken(userName);//            return token;            return userByUserName;        } else {            // Mật khẩu không hợp lệ hoặc tài khoản không tồn tại            return null;        }    }}