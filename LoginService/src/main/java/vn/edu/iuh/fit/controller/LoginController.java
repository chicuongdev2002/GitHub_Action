package vn.edu.iuh.fit.controller;import org.springframework.http.HttpStatus;import org.springframework.http.ResponseEntity;import org.springframework.web.bind.annotation.*;import org.springframework.web.client.RestTemplate;import vn.edu.iuh.fit.enity.User;@RestController@RequestMapping("/login")public class LoginController {    private RestTemplate restTemplate=new RestTemplate();    @GetMapping("hello")    public String hello() {        return "Hello";    }   @GetMapping("/2a")    public User getUser(@RequestBody User user){        return restTemplate.postForObject("http://localhost:8805/auth/login",user,User.class);    }    @GetMapping("/2b")    public ResponseEntity<String> login(@RequestBody User user) {        // Gửi yêu cầu POST để đăng nhập và nhận kết quả từ server        ResponseEntity<String> response = restTemplate.postForEntity("http://localhost:8805/auth/login", user, String.class);        System.out.println(response);        // Kiểm tra mã trạng thái của phản hồi từ server        if (response.getStatusCode().is2xxSuccessful()) {            // Nếu đăng nhập thành công, trả về thông báo thành công cho client            return ResponseEntity.ok("Login success");        } else {            // Nếu đăng nhập thất bại, trả về thông báo lỗi cho client            return ResponseEntity.status(HttpStatus.UNAUTHORIZED).body("Login failed");        }    }}